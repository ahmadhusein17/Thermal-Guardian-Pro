<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light"
        android:icon="@android:drawable/ic_menu_info_details">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Official AccessibilityService declaration, per
          https://developer.android.com/guide/topics/ui/accessibility/service
          Requires BIND_ACCESSIBILITY_SERVICE and can only be turned on by
          the user, explicitly, in Settings > Accessibility - this cannot
          be automated by the app or by the module, by Android design.
        -->
        <service
            android:name=".ForegroundBridgeService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
